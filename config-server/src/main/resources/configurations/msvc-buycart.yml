# application.yml for msvc-buycart
server:
  port: 9003

spring:
  application:
    name: msvc-buycart
  datasource:
    url: jdbc:mysql://localhost:3307/buycart?useSSL=false&serverTimezone=UTC
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
